def foo
  M.log("#{M.property_list.inspect}\n")
  M.set_property("chapter-list", [])

  %w(filename chapter-list).each do |p|
    M.log([p, M.get_property(p)].inspect + "\n")
  end

  M.set_property("time-pos", 633)
  M.set_property("mute", true)
end

def bar
  foo
end

def baz
  bar
end

baz
